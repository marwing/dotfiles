include variables

# You can get the names of your inputs by running: swaymsg -t get_inputs
# Read `man 5 sway-input` for more information about this section.

input type:touchpad {
  dwt enabled
  tap enabled
  natural_scroll enabled
  middle_emulation enabled
}

input type:keyboard {
  repeat_delay 250
  xkb_layout de
  # xkb_capslock disabled # currently crashes firefox (see: https://bugzilla.mozilla.org/show_bug.cgi?id=1652820)
  # xkb_numlock enabled
}

seat * {
  hide_cursor 2000
}
